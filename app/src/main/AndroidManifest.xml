<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.supermart.supermart">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterActivity"
            android:label="Pendaftaran" />
        <activity
            android:name=".DaftarProdukActivity"
            android:label="Pilih Produk" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".DaftarPesananActivity"
            android:label="Pesanan Saya" />
        <activity
            android:name=".DaftarDiCartActivity"
            android:label="Troli Saya" />
        <activity android:name=".DaftarActivityTab" />
        <activity
            android:name=".DaftarKategoriActivity"
            android:label="Pilih Kategori" />
        <activity
            android:name=".PilihAlamatActivity"
            android:label="Pilih Alamat" />
        <activity
            android:name=".RincianPesananActivity"
            android:label="Rincian Pesanan" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".PilihLokasiActivity"
            android:label="@string/title_activity_pilih_lokasi" >
            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.MAIN" />-->

                <!--<category android:name="android.intent.category.LAUNCHER" />-->
            <!--</intent-filter>-->
        </activity>
        <activity android:name=".SelamatDatangActivity" />
        <activity android:name=".KonfirmasiPesananActivity"></activity>
    </application>

</manifest>